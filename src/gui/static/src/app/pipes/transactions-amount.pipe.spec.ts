import { TransactionddoosuntPipe } from './transactions-amount.pipe';

describe('TransactionddoosuntPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionddoosuntPipe();
    expect(pipe).toBeTruthy();
  });
});
